# powershell like terminal in ubuntu 24.04

these are settings file that allows to quickly configure zsh, ghostty and starship. 

the current configuration has been designed to works like powershell.

the export-files script copy the files from local directory to common directories where the configs are saved, this is my attempt to optimizing the workflow 